#include <cmath>
#include <iostream>
#include <string>

using namespace std;

/* función principal */
int main()
{
    string a, b, n, k;
    cout << (unsigned long long)pow(2.0, 64) << endl;
    return 0;
}
